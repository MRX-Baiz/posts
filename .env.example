# Django Configuration
SECRET_KEY=your-secret-key-here-generate-a-new-one
DEBUG=False

# Database (provided automatically by Render when you add PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/dbname

# Email Configuration (if using password reset feature)
EMAIL_PASSWORD=your-gmail-app-specific-password

# Note: ALLOWED_HOSTS and RENDER_EXTERNAL_HOSTNAME are automatically set by Render
# You don't need to configure them manually
